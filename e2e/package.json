{
  "name": "stanza-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:local": "BASE_URL=http://localhost:23710 playwright test",
    "test:prod": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  }
}
